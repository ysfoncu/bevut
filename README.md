
  # Replicate UI Design

  This is a code bundle for Replicate UI Design. The original project is available at https://www.figma.com/design/kHDxZB6qADDvnpIN9Mi7RO/Replicate-UI-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  